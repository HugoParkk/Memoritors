<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
        h1 {
            margin-top: 0;
            background-color: #ccc;
            height: 80px;
            padding: 10px;
            line-height: 80px;
            font-size: 40px;
            margin-bottom: 0;
        }

        .mdeContainer {
            display: inline-block;
            vertical-align: top;
            width: 100%;
        }

        .mdeContainer CodeMirror {
            height: 1000px;
        }

        button {
            background-color: rgb(218, 218, 218);
            border: 1px solid rgb(218, 218, 218);
            border-radius: 10px;
            width: 40px;
            height: 40px;
            font-size: 40px;
            line-height: 10px;
        }

        select {
            border: 1px solid rgb(104, 104, 104);
            border-radius: 10px;
            font-size: 20px;
            width: 200px;
            height: 50px;
        }

        .line {
            margin-top: 100px;
            width: 2px;
            height: 800px;
            background-color: #ccc;
        }

        .grid2 {
            margin-left: 100px;
            padding-left: 50px;
            border-left: 1px solid rgb(90, 90, 90);
        }

        #container {
            display: grid;
            grid-template-columns: 150px 1fr;
        }

        .user {
            font-size: 20px;
        }

        textarea {
            width: 100px;
        }

        .grid1 {
            margin: 10px;
        }

        #profileImg {
            width: 8vh;
            height: 8vh;
            border-radius: 10000px;
        }
    </style>

    <link rel="stylesheet" href="./style/common.css">
    <script src="https://cdn.jsdelivr.net/simplemde/latest/simplemde.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
</head>

<body>
    <div id="container">
        <div class="grid1">
            <img id="profileImg" src="http://localhost:3000/uploadImage/<%= user.profileImg %> " alt="">
            <form action="/uploadImage/<%= user.name %>" enctype="multipart/form-data" method="post">
                <input type="file" name="attachment">
                <button type="submit">Upload</button>
            </form>
            <p class="user"><%= user.name %> </p>
            <form action="/signout" method="post">
                <button>Logout</button>
            </form>
            <p><button>+</button>
                <button>-</button>
            </p>
            <select name="list" class="list">
                <option value="">Memo 1</option>
                <option value="">Memo 2</option>
                <option value="">Memo 3</option>
            </select>
        </div>
        <div class="grid2">
            <h1>Memo 1</h1>
            <div class="mdeContainer">
                <textarea id="editor"></textarea>
            </div>
        </div>
    </div>

    <script src="./script/index.js"></script>
</body>

</html>